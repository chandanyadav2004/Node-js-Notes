<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <!-- Tailwind CDN (correct way) -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
    <a href="/" class="text-blue-500">Back to Home</a>
    <h1 class="text-3xl mt-10 tracking-tighter mb-3">All Users</h1>
    <div class="users flex gap-4 flex-wrap ">
        <% if(users.length>0){ %>
            <% users.forEach(function(user){ %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-52 rounded-lg bg-zinc-500 overflow-hidden">
                        <img src="<%= user.image %>" alt="" class="w-full h-full object-top  object-cover">
                    </div>
                    <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                    <h5 class="text-zinc-500 "><%= user.email %></h5>
                    <div class="flex gap-3 mt-5">
                        <a class="text-zinc-700" href="">Edit Users</a>
                        <a class="text-red-500" href="/delete/<%= user._id %>">Delete Users</a>
                    </div>
                </div>
            <% }) %>

        <% } else { %>

            <h3 class="text-zinc-500 ">No Users yet</h3>

        <% } %>


       
    </div>
    
    
  </div>
</body>
</html>
